---
"@runfusion/fusion": minor
---

summary: Let operators choose the language agents use for replies, reports, task logs, and completion summaries.
category: feature
dev: Global `operatorLanguage` setting (default "auto" = today's behavior). A prompt directive is injected into the dashboard chat system prompt and the heartbeat lane system prompt; deterministic `system` message templates remain untranslated.
